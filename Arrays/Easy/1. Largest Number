int largest_Element(vector<int> &arr){
  int n = arr.size();
  int maxm = arr[0];

  for(int i=1;i<n;i++){
    if(arr[i]>maxm)
      maxm = arr[i];
  }
  return maxm;
}

int main(){
vector<int> arr = {16,25,34,18,22};
cout<<"Largest Element in the list: "<<largest_Element(arr);
return 0;
}
