int maximum_Ones(int arr[],int n){
  int count = 0;
  int maxm_count = 0;
  for(int i=0;i<n;i++){
    if(arr[i]==1){
      count++;
      maxm_count = max(maxm_count,count);
    }
    else{
      count=0;
    }
  }
return maxm_count;
}
