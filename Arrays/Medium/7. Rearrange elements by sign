void rearrange(vector<int> &arr){
    int n = arr.size();
    vector<int> pos,neg;
    
    for(auto it: arr){
      if(it>0) pos.push_back(it);
      else neg.push_back(it);
    }
    
    for(int i=0;i<n/2;i++){
        arr[2*i]=pos[i];
        arr[2*i+1]=neg[i];
    }
}
