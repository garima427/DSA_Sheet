int maxProductSubarray(int arr[],int n){
  int maxProduct = arr[0];
  int pre = 1, suff = 1;
  for(int i=0; i<n; i++){
    if(pre==0) pre = 1;
    if(suff == 0) suff = 1;
    
    pre*=arr[i];
    suff*=arr[n-i-1];

    maxProduct = max(pre,suff);
  }
  return maxProduct;
}
