void mergeArrays(int arr1[], int n, int arr2[], int m){
  int l = n-1, r = 0;
  while(l>=0 && r<m){
    if(arr1[l]>arr2[r]){
      swap(arr1[l],arr2[r]);
      l--;
      r++;
    }
    else{
      break;
    }
  }

  sort(arr1,arr1+n);
  sort(arr2,arr2+m);
}
