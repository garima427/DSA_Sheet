int findMax(Node* root){
    if(!root)
        return INT_MIN;

    while(root->right){
        root = root->right;
    }

    return root->data;
}

int findMin(Node* root){
    if(!root)
        return INT_MAX;
    
    while(root->left){
        root = root->left;
    }
    return root->data;
}
