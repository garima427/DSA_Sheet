Node* reverse(Node* head){
  Node* prev = nullptr;
  Node* curr = head;
  Node* front;
  while(curr){
    front = curr->next;
    curr->next = prev;
    prev = curr;
    curr = front;
  }
  return prev;
}
